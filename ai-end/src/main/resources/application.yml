spring:
  profiles:
    active: dev
  application:
    name: ai-end

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.example.aiend: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
