# 亲情代理"代TA操作"功能实施计划

## 功能概述

在亲情绑定页面,子女用户点击"代TA操作"后,可以切换到代理模式,以被绑定老人的身份发布任务。这样子女可以帮助不熟悉手机操作的老人发布服务需求。

## 现状分析

### 已实现功能
1. **亲情绑定流程**：子女可以申请绑定老人账号,审核通过后建立绑定关系
2. **代理模式切换**：前端已有 `toggleProxyMode` 功能,可以切换代理状态
3. **代理Token机制**：后端会返回 `proxyToken`,存储在 localStorage
4. **前端状态管理**：使用 AuthContext 管理代理状态 (`isProxyMode`, `proxyTarget`)

### 需要实现的功能
当前代理模式只是切换了状态,但**实际发布任务时还没有使用代理身份**。需要：
1. 在代理模式下,所有API请求都应该携带 `proxyToken` 而非普通 `token`
2. 后端需要解析 `proxyToken`,以被代理人的身份执行操作
3. 前端需要在代理模式下显示提示,让用户知道当前是在代理谁

## 用户审核需求

> [!IMPORTANT]
> **关键设计决策**
> 
> 本计划采用**JWT Token替换**方案实现代理功能,即：
> - 开启代理模式时,后端返回包含被代理人信息的新 `proxyToken`
> - 前端将 `proxyToken` 替换原有 `token` 发送给后端
> - 后端解析 `proxyToken` 获取实际操作人ID（被代理人ID）
> 
> **替代方案**：在请求头中同时发送 `token` 和 `proxyToken`
> - 优点：token职责更清晰
> - 缺点：需要改造所有后端接口的权限验证逻辑
> 
> 请确认是否采用当前的 Token 替换方案。

> [!WARNING]
> **权限边界**
> 
> 代理模式下,子女能够以老人身份执行的操作范围**仅限于**：
> - ✅ 发布任务（消耗老人的积分）
> - ✅ 查看老人的订单（我接的、我发布的）
> - ✅ 查看老人的积分余额
> - ✅ 使用老人的积分兑换实物
> 
> **不允许**的操作：
> - ❌ 修改老人的个人信息（头像、昵称、手机号等）
> - ❌ 修改老人的密码
> - ❌ 解绑亲情关系
> - ❌ 接取任务（以老人身份做志愿者）
> - ❌ 申诉、评价等其他敏感操作
> 
> **实施说明**：后端需要在权限验证中间件中检查 `proxyToken`,仅允许访问白名单接口。

## 计划变更

### 后端接口变更

#### 1. 代理Token生成逻辑（已有接口,需调整）

**接口路径**: `POST /api/family/proxy/toggle`

**现有实现**: 已返回 `proxyToken`

**调整需求**: 确保 `proxyToken` 的 JWT payload 包含以下信息：

```json
{
  "userId": 1001,           // 被代理人ID（老人ID）
  "realUserId": 2001,       // 实际操作人ID（子女ID）
  "isProxy": true,          // 代理标识
  "exp": 1234567890         // 过期时间
}
```

#### 2. 后端权限验证中间件调整

**调整内容**:
- 后端接收到 `proxyToken` 时,解析 JWT 获取 `userId`（被代理人）和 `realUserId`（子女）
- 检查请求的接口是否在代理模式白名单中
- 如果在白名单中,使用 `userId` 作为当前操作用户ID执行业务逻辑
- 如果不在白名单中,返回 403 Forbidden 错误
- 记录 `realUserId` 用于审计日志

**代理模式接口白名单**:
```
任务相关:
- POST /api/task/publish - 发布任务
- GET /api/task/my-published - 查看我发布的任务
- GET /api/task/my-accepted - 查看我接的任务（虽然老人不太会接任务,但需要支持）

用户相关:
- GET /api/user/balance - 查询余额

商城相关:
- GET /api/mall/products - 查看商品列表
- POST /api/mall/exchange - 兑换商品
- GET /api/mall/orders - 查看兑换订单
```

---

### 前端实现变更

#### [MODIFY] [Family.tsx](file:///c:/Users/CaTeam/Desktop/ai%20project/timebank-family/pages/Family.tsx)

**变更内容**:
- 在代理模式激活时,页面顶部显示明显的代理提示横幅
- 提示内容：`正在代理 {被代理人昵称} 操作`
- 提供"退出代理"快捷按钮

**示例实现**:
在 Family.tsx 组件的 render 方法中,在主内容区域顶部添加代理提示横幅。使用橙色边框和背景色突出显示,包含代理对象信息和快捷退出按钮。

---

#### [MODIFY] [services/taskApi.ts](file:///c:/Users/CaTeam/Desktop/ai%20project/timebank-family/services/taskApi.ts)

**变更内容**:
- 在 API 请求时,检查是否存在 `proxyToken`
- 如果存在,使用 `proxyToken` 替换 `Authorization` header 中的 token
- 如果不存在,使用普通 token

**实现逻辑**:
创建一个辅助函数 `getAuthToken`,优先获取 proxyToken,如果不存在则获取普通 token。在所有 API 请求的 Authorization header 中使用此函数返回的 token。

---

#### [MODIFY] [pages/TaskPublish.tsx](file:///c:/Users/CaTeam/Desktop/ai%20project/timebank-family/pages/TaskPublish.tsx)

**变更内容**:
- 在任务发布页面顶部也显示代理提示（如果处于代理模式）
- 让用户明确知道是在为谁发布任务
- 发布成功后的提示也应体现代理关系

---

### 数据库变更

**无需变更**

现有的数据库表已经满足需求：
- `sys_user`: 存储用户信息
- `sys_user_relation`: 存储绑定关系
- `tb_task`: 发布任务时 `publisher_id` 会是被代理人ID
- `tb_coin_log`: 积分流水会记录在被代理人账户

---

## 验证计划

### 自动化测试

**暂无自动化测试**

项目目前没有自动化测试框架,建议后续添加。

### 手动验证步骤

#### 测试环境准备
1. 准备两个账号：
   - 账号A（老人）：手机号 138xxxx8000,有足够积分
   - 账号B（子女）：手机号 139xxxx9000
2. 使用账号B绑定账号A（需要完成审核流程）

#### 测试步骤

**步骤1：开启代理模式**
1. 使用账号B登录
2. 进入"亲情账号"页面
3. 点击账号A的"代TA操作"按钮
4. 验证：
   - [ ] 按钮文字变为"退出代理"
   - [ ] 按钮样式变为橙色高亮
   - [ ] 页面顶部显示代理提示横幅："正在代理 {账号A昵称} 操作"
   - [ ] localStorage 中存储了 `proxyToken`

**步骤2：代理发布任务**
1. 保持代理模式,进入"任务发布"页面
2. 验证页面顶部有代理提示
3. 填写任务信息：
   - 标题："帮忙买菜"
   - 类型：跑腿
   - 悬赏：20积分
   - 其他必填信息
4. 点击发布
5. 验证：
   - [ ] 任务发布成功
   - [ ] 成功提示中体现代理关系（如："已为 {账号A昵称} 发布任务"）

**步骤3：验证任务归属**
1. 退出代理模式
2. 使用账号A登录
3. 进入"我的订单" -> "我发布的"
4. 验证：
   - [ ] 能看到刚才发布的任务
   - [ ] 账号A的积分已扣除（或冻结）

**步骤4：退出代理模式**
1. 使用账号B登录
2. 进入"亲情账号"页面
3. 点击"退出代理"按钮或再次点击"代TA操作"
4. 验证：
   - [ ] 按钮文字恢复为"代TA操作"
   - [ ] 代理提示横幅消失
   - [ ] localStorage 中的 `proxyToken` 被清除

**步骤5：验证普通模式**
1. 保持账号B登录,已退出代理模式
2. 进入"任务发布"页面
3. 发布一个新任务
4. 验证：
   - [ ] 任务发布在账号B名下
   - [ ] 账号B的积分被扣除

---

## 风险评估

### 技术风险

1. **Token过期问题**
   - 风险：`proxyToken` 可能在使用过程中过期
   - 缓解：设置合理的过期时间（建议2小时）,前端检测401响应后提示重新开启代理

2. **并发代理问题**
   - 风险：同一子女同时代理多个老人（虽然产品设计上不允许）
   - 缓解：前端只允许单个代理模式,切换时自动关闭之前的代理

### 业务风险

1. **误操作风险**
   - 风险：子女在代理模式下误以为是自己的账号,发布错误任务
   - 缓解：在所有关键页面都显示明显的代理提示

2. **权限越界风险**
   - 风险：后端未正确实现权限控制,导致代理模式下可以执行不应执行的敏感操作
   - 缓解：后续迭代中添加权限白名单机制

---

## 实施顺序

建议按以下顺序实施：

1. **前置确认**（当前阶段）
   - 确认技术方案（Token替换 还是 双Token）
   - 确认权限范围

2. **后端开发**
   - 调整 `proxyToken` 生成逻辑,确保包含必要字段
   - 调整权限验证中间件,支持 `proxyToken` 解析

3. **前端开发**
   - 更新 `taskApi.ts` 的 token 获取逻辑
   - 在 [Family.tsx](file:///c:/Users/CaTeam/Desktop/ai%20project/timebank-family/pages/Family.tsx) 添加代理提示横幅
   - 在 `TaskPublish.tsx` 添加代理提示

4. **文档更新**
   - 更新 `亲情代理接口文档.md`
   - 更新 `任务发布接口文档.md`

5. **测试验证**
   - 执行手动测试步骤
   - 修复发现的问题

---

**文档版本**: v1.0  
**创建时间**: 2026-02-12
